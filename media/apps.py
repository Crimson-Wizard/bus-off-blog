from django.apps import AppConfig


class GalleryConfig(AppConfig):
    """
    App configuration for the Gallery application.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'media'
